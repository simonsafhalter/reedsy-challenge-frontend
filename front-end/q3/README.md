This project demonstrates styling the static `q3.html` with SASS.

To compile SASS to CSS run

```
npm run style
```

To compile SASS to CSS and start a watcher that will compile on every detected change run

```
npm run style-watch
```
